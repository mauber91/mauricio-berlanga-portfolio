# Writing content

This directory is ready for future MDX articles. Add an MDX integration when the first article is published; until then, article metadata lives in `src/data/content.ts` to keep the production bundle minimal.
